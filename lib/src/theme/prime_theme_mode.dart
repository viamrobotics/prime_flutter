/// Describes which theme will be used by [PrimeApp].
enum PrimeThemeMode {
  /// Use either the light or dark theme based on the system setting.
  system,

  /// Always use the light theme.
  light,

  /// Always use the dark theme.
  dark,
}
